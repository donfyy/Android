<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.donfyy.viewexample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".canvas.TextMultiDrawActivity"></activity>
        <activity android:name=".canvas.BitmapDrawActivity" />
        <activity android:name=".canvas.MatrixActivity" />
        <activity android:name=".canvas.XfermodesActivity" />
        <activity android:name=".canvas.TransformationActivity" />
        <activity android:name=".canvas.StackActivity" />
        <activity android:name=".canvas.DrawActivity" />
        <activity android:name=".canvas.CanvasComposeActivity" />
        <activity android:name=".canvas.CanvasActivity" />
        <activity
            android:name=".LottieActivity"
            android:label="@string/title_activity_lottie"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VectorDrawableActivity" />
    </application>

</manifest>